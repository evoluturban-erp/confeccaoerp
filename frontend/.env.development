# Desenvolvimento local
# Vite proxeia /api → localhost:3001 automaticamente (vite.config.js)
# Deixar em branco usa o proxy e evita CORS; definir a URL direta também funciona
VITE_API_URL=http://localhost:3001/api
